#include "main.h"
#include <stdio.h>
/**
 * main - program that prints its name followed by a new lline
 * @argc: number of strings in the argv array
 * @argv: this is the array containing the arguments for this function
 * Return: 0
 */

int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
