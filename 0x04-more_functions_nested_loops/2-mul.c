#include "main.h"
/**
 * mul - will return multiplication result
 * @a: first int to be multiplied
 * @b: second integer to be multiplid
 * Return: the multiplication reslult
 */
int mul(int a, int b)
{
	return (a*b);
}
